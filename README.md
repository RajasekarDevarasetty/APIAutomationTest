# This is APIAutomationTest using Java and RestAssured with BDD framework using Cucumber
# Below command are used to run these automation scripts
# ./gradlew test
# ./gradlew cleanTest test --tests Runner